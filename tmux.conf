# 开启鼠标支持
set -g mouse on

# 配置快捷键前缀
unbind C-b
set -g prefix C-a
bind C-a send-prefix

# 垂直分割
bind | split-window -h

# 水平分割
bind - split-window -v

# 重新加载配置
bind r source-file ~/.tmux.conf \; display "配置已重新加载"
